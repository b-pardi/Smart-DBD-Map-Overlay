name: dbdmap-env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - tesserocr
  - libblas=*=*openblas
  - numpy
  - pillow
  - requests
  - tqdm
  - pip
  - pip:
      - mss
      - customtkinter
      - pyinstaller
